interface:
  display_name: "Standardize SCI Images"
  short_description: "Standardize microscopy images with audit trails"
  default_prompt: "Use $standardize-sci-images to standardize my microscopy image batch with locked display settings, calibrated scale bars, and an audit trail."
