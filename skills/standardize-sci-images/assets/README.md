# Assets

The skill uses no hidden LUT or image template. LUT mappings and scale-bar geometry are implemented explicitly in `scripts/standardize_images.py`, so every display operation can be audited.
